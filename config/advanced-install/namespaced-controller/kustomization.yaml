#
# Controller only with namespaced install, witout CRD
#

apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../base/shared-config
  - ../../base/controller-manager
  - ../../namespace-install/rbac/controller-manager

images:
  - name: quay.io/kynoproj/kynomesh
    newTag: latest

patches:
  # For release scripts
  - patch: |-
      apiVersion: apps/v1
      kind: Deployment
      metadata:
        name: kynomesh-controller
      spec:
        template:
          spec:
            containers:
              - name: controller-manager
                env:
                  - name: KYNOMESH_IMAGE
                    value: quay.io/kynoproj/kynomesh:latest
  - patch: |-
      apiVersion: v1
      kind: ConfigMap
      metadata:
        name: kynomesh-cmd-params-config
      data:
        namespaced: "true"
