apiVersion: v1
kind: ConfigMap
metadata:
  name: kynomesh-controller-config
data:
  controller-config.yaml: |+
    defaults:
      containerResources: |
        requests:
          memory: "128Mi"
          cpu: "100m"
